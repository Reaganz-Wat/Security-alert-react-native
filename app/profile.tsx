import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

export default function ProfileScreen() {
    return (
        <ThemedView>
            <ThemedText>Hello, this is profile screen</ThemedText>
        </ThemedView>
    )
}