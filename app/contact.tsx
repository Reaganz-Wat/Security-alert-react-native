import { ThemedText } from "@/components/ThemedText";
import { ThemedView } from "@/components/ThemedView";

export default function ContactScreen() {
    return(
        <ThemedView>
            <ThemedText>This is a themed text for contact</ThemedText>
        </ThemedView>
    )
}